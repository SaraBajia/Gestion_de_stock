<Window x:Class="WpfApp1.Window8"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestion de Stock IT" Height="750" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5">

    <Window.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCDDEE"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderThickness="0" CornerRadius="6" Margin="8,2">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Margin="10,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A4A7A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarButtonActiveStyle" TargetType="Button"
               BasedOn="{StaticResource SidebarButtonStyle}">
            <Setter Property="Background" Value="#1A4A7A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterRadioStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="White" BorderBrush="#D0D5DD"
                                BorderThickness="1" CornerRadius="20" Padding="12,5">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0B3D6B"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#0B3D6B"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0B3D6B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#D0D5DD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E6EA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EAECEF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="ColumnHeaderHeight" Value="36"/>
            <Setter Property="RowHeight" Value="34"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0B3D6B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#0A3560"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#0B3D6B">
            <DockPanel LastChildFill="True">
                <Border DockPanel.Dock="Top" Background="#092F52" Padding="20,18">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="BtnRetour"
                                    Content="&#xE76B;"
                                    FontFamily="Segoe MDL2 Assets"
                                    FontSize="13"
                                    Foreground="White"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Padding="0"
                                    Width="18" Height="18"
                                    Margin="0,0,4,0"
                                    Command="{Binding RetourCommand}"
                                    ToolTip="Retour"/>
                            <TextBlock Text="AIRBUS" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="Gestion de Stock IT" Foreground="#88AACC" FontSize="10" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>

                <Border DockPanel.Dock="Bottom" Background="#092F52" Padding="16,16">
                    <StackPanel>
                        <TextBlock x:Name="txtUserName" Text="{Binding TxtUserName}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock x:Name="txtUserEmail" Text="{Binding TxtUserEmail}" Foreground="#88AACC" FontSize="10" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                        <Button Content="Se déconnecter" Margin="0,12,0,0" Style="{StaticResource ActionButtonStyle}" Background="#C0392B" Foreground="White" FontSize="11" Command="{Binding DeconnecterCommand}"/>
                    </StackPanel>
                </Border>

                <StackPanel Margin="0,15,0,0">
                    <Button Style="{StaticResource SidebarButtonStyle}" Margin="0,4" Command="{Binding AccueilCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏠" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="Accueil"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonActiveStyle}" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📦" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="Stock"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnUtilisateurs" Style="{StaticResource SidebarButtonStyle}" Margin="0,4" Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}" Command="{Binding UtilisateursCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👤" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="Utilisateurs"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}" Margin="0,4" Command="{Binding MaterielCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🖥" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="Matériel"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}" Margin="0,4" Command="{Binding ConsommableCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🖨" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="Consommable"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}" Margin="0,4" Command="{Binding PieceRechangeCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔧" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="Pièce de rechange"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}" Margin="0,4" Command="{Binding MvtCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" FontSize="14" Margin="0,0,10,0"/>
                            <TextBlock Text="Mouvements"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" Padding="20,14" BorderBrush="#E2E6EA" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Stock" FontSize="18" FontWeight="Bold" Foreground="#0B3D6B" FontFamily="Segoe UI"/>
                        <TextBlock Text="Liste générale du stock informatique" FontSize="11" Foreground="#888" FontFamily="Segoe UI"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="btnAjouter" Content="+ Ajouter" Style="{StaticResource ActionButtonStyle}" Background="#0B3D6B" Foreground="White" Margin="0,0,8,0" Command="{Binding AjouterCommand}"/>
                        <Button x:Name="btnRetirer" Content="− Retirer" Style="{StaticResource ActionButtonStyle}" Background="#E74C3C" Foreground="White" Margin="0,0,8,0" Command="{Binding RetirerCommand}"/>
                        <Button x:Name="btnModifier" Content="✎ Modifier" Style="{StaticResource ActionButtonStyle}" Background="#F39C12" Foreground="White" Margin="0,0,8,0" Command="{Binding ModifierCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="White" Padding="20,12,20,10" BorderBrush="#E2E6EA" BorderThickness="0,0,0,1" Margin="0,0,0,4">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Rechercher par :" VerticalAlignment="Center" FontSize="12" Foreground="#555" FontFamily="Segoe UI" Margin="0,0,8,0"/>

                        <ComboBox x:Name="cmbRechercherPar" ItemsSource="{Binding SearchFieldOptions}" SelectedItem="{Binding SelectedSearchField, Mode=TwoWay}" Grid.Column="1" Height="34" FontSize="12" FontFamily="Segoe UI" VerticalContentAlignment="Center" BorderBrush="#D0D5DD" Background="White"/>

                        <Border Grid.Column="3" Background="#F5F7FA" BorderBrush="#D0D5DD" BorderThickness="1" CornerRadius="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="txtPlaceholder" Grid.Column="0" Text="Saisir un mot-clé..." Foreground="#AAAAAA" FontSize="12" FontFamily="Segoe UI" VerticalAlignment="Center" Margin="10,0" IsHitTestVisible="False" Panel.ZIndex="1" Visibility="{Binding PlaceholderVisible, Converter={StaticResource BoolToVis}}"/>
                                <TextBox x:Name="txtSearch" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Grid.Column="0" Padding="10,6" BorderThickness="0" Background="Transparent" FontSize="12" VerticalAlignment="Center" FontFamily="Segoe UI" Panel.ZIndex="2"/>
                                <TextBlock Grid.Column="1" Text="🔍" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="13" Foreground="#888"/>
                            </Grid>
                        </Border>

                        <Button Grid.Column="4" Content="Rechercher" Style="{StaticResource ActionButtonStyle}" Background="#0B3D6B" Foreground="White" Height="34" Margin="8,0,0,0" Command="{Binding RechercherCommand}"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Filtrer :" VerticalAlignment="Center" FontSize="11" Foreground="#888" FontFamily="Segoe UI" Margin="0,0,10,0"/>
                        <RadioButton Content="Tous" Style="{StaticResource FilterRadioStyle}" IsChecked="True" GroupName="FilterType" Command="{Binding FilterCommand}" CommandParameter="Tous"/>
                        <RadioButton Content="PC portable" Style="{StaticResource FilterRadioStyle}" GroupName="FilterType" Command="{Binding FilterCommand}" CommandParameter="PC portable"/>
                        <RadioButton Content="PC fixe" Style="{StaticResource FilterRadioStyle}" GroupName="FilterType" Command="{Binding FilterCommand}" CommandParameter="PC fixe"/>
                        <RadioButton Content="Tablette" Style="{StaticResource FilterRadioStyle}" GroupName="FilterType" Command="{Binding FilterCommand}" CommandParameter="Tablette"/>
                        <RadioButton Content="Téléphone" Style="{StaticResource FilterRadioStyle}" GroupName="FilterType" Command="{Binding FilterCommand}" CommandParameter="Téléphone"/>
                        <RadioButton Content="Consommable" Style="{StaticResource FilterRadioStyle}" GroupName="FilterType" Command="{Binding FilterCommand}" CommandParameter="Consommable"/>
                        <RadioButton Content="Pièce de rechange" Style="{StaticResource FilterRadioStyle}" GroupName="FilterType" Command="{Binding FilterCommand}" CommandParameter="Pièce de rechange"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Margin="16,4,16,8" CornerRadius="6" BorderBrush="#E2E6EA" BorderThickness="1" Background="White">
                <DataGrid x:Name="dgStock" ItemsSource="{Binding DataView}" SelectedItem="{Binding SelectedRow, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
              CanUserResizeRows="False" ColumnWidth="*"
              ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                </DataGrid>
            </Border>
            <Border Grid.Row="3" x:Name="panelModifier" Background="White" Margin="16,0,16,12" CornerRadius="6" BorderBrush="#D0D5DD" BorderThickness="1" Padding="16,12" Visibility="{Binding PanelModifierVisible, Converter={StaticResource BoolToVis}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="Modifier l'enregistrement" FontWeight="SemiBold" FontSize="13" Foreground="#0B3D6B" FontFamily="Segoe UI"/>
                        <Button DockPanel.Dock="Right" Content="✕" Background="Transparent" BorderThickness="0" Foreground="#999" FontSize="14" Cursor="Hand" HorizontalAlignment="Right" Command="{Binding FermerModifierCommand}"/>
                    </DockPanel>

                    <UniformGrid Grid.Row="1" Rows="1" Columns="5" Margin="0,0,0,10">
                        <StackPanel x:Name="GroupEtiquette" Margin="0,0,10,0">
                            <TextBlock x:Name="lblEtiquette" Text="{Binding LblEtiquette}" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditEtiquette" Text="{Binding EditEtiquette, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}" Background="#F5F7FA"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupType" Margin="0,0,10,0" Visibility="{Binding GroupTypeVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Type" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditType" Text="{Binding EditType, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding EditTypeReadOnly}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupNom" Margin="0,0,10,0" Visibility="{Binding GroupNomVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Nom" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditNom" Text="{Binding EditNom, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupModele" Margin="0,0,10,0" Visibility="{Binding GroupModeleVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Modèle" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditModele" Text="{Binding EditModele, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupRAM" Visibility="{Binding GroupRAMVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="RAM" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditRAM" Text="{Binding EditRAM, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>
                    </UniformGrid>

                    <UniformGrid Grid.Row="2" Rows="1" Columns="5" Margin="0,0,0,10">
                        <StackPanel x:Name="GroupStockage" Margin="0,0,10,0" Visibility="{Binding GroupStockageVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Stockage" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditStockage" Text="{Binding EditStockage, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupNumSerie" Margin="0,0,10,0" Visibility="{Binding GroupNumSerieVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Num. Série" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditNumSerie" Text="{Binding EditNumSerie, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupMarque" Margin="0,0,10,0" Visibility="{Binding GroupMarqueVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Marque" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditMarque" Text="{Binding EditMarque, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupProcesseur" Margin="0,0,10,0" Visibility="{Binding GroupProcesseurVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Processeur" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditProcesseur" Text="{Binding EditProcesseur, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupAdrMac" Visibility="{Binding GroupAdrMacVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Adresse MAC" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditAdrMac" Text="{Binding EditAdrMac, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>
                    </UniformGrid>

                    <UniformGrid Grid.Row="3" Rows="1" Columns="5" Margin="0,0,0,12">
                        <StackPanel x:Name="GroupCouleur" Margin="0,0,10,0" Visibility="{Binding GroupCouleurVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock x:Name="lblCouleur" Text="{Binding LblCouleur}" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditCouleur" Text="{Binding EditCouleur, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupReference" Margin="0,0,10,0" Visibility="{Binding GroupReferenceVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Référence" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditReference" Text="{Binding EditReference, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupQuantite" Margin="0,0,10,0" Visibility="{Binding GroupQuantiteVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Quantité" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditQuantite" Text="{Binding EditQuantite, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                        </StackPanel>

                        <StackPanel x:Name="GroupDate" Visibility="{Binding GroupDateVisible, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Date" FontSize="11" Foreground="#666" Margin="0,0,0,3"/>
                            <TextBox x:Name="txtEditDate" Text="{Binding EditDate}" Style="{StaticResource FormTextBoxStyle}" IsReadOnly="True" Background="#F5F7FA"/>
                        </StackPanel>
                    </UniformGrid>

                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="💾 Enregistrer" Style="{StaticResource ActionButtonStyle}" Background="#27AE60" Foreground="White" Height="32" Width="160" Command="{Binding EnregistrerCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>